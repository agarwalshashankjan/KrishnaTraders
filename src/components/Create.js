import React from 'react';
import {View, Text} from 'react-native';
import Form from './Form';

const Create = () => {
  return (
    <View>
      <Form />
    </View>
  );
};

export default Create;
